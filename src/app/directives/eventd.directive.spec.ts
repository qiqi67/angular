import { EventdDirective } from './eventd.directive';

describe('EventdDirective', () => {
  it('should create an instance', () => {
    const directive = new EventdDirective();
    expect(directive).toBeTruthy();
  });
});
